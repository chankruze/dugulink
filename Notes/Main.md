**Command Format**

```text
<COMMAND>:<CHANNEL_PIN>:<VALUE>\n
```

Examples:
ADD:3\n → Add a channel connected to pin 3.
REMOVE:4\n → Remove a channel on pin 4.
SET:3:ON\n → Turn ON the channel on pin 3.
SET:13:OFF\n → Turn OFF the channel on pin 3.
LIST\n → Request a list of active channels.
